如果直接在自己的项目文件下生产了和URL中一样的目录文件，比如访问：yourdomin.com/product/1.html,
你的项目文件夹下真的存在product/1.html这个路径，那么IIS会直接去请求这个静态页面，
如果项目中使用了自定义的管道处理程序，那么这个静态页仍然会走我们的自定义管道处理程序，
我们可以在这里通过缓存来实现要不要重新成长静态页或删除过期产品的静态页，如果不使用此方法，
只能去写执行计划，定时跑这些静态文件了，修改Application_BeginRequest